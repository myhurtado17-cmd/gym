# Vercel Postgres / Neon connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Used later for auth + session signing
AUTH_SECRET="change-me"

# Optional
NODE_ENV="development"
